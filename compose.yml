services:

  fastapi:
    build: .
    image: mt-evaluate

    ports:
      - "8000:8000"

    env_file:
      - .env

  prometheus:
    image: prom/prometheus

    ports:
      - "9090:9090"

    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
